# Dependências
node_modules/
.pnp
.pnp.js

# Next.js
/.next/
/out/
next-env.d.ts

# Produção
/build
*.tsbuildinfo

# Ambiente
.env
.env*.local

# Dados de negócios/usuários reais (nunca versionar)
/data

# Backups locais do Postgres self-hosted (GH-OPS Bloco 2) — nunca versionar
# dump de banco com dado de empresa real.
/deploy/backups
# `deploy/supabase/.env` já cai na regra `.env` acima (sem barra = qualquer
# pasta); explícito aqui só para quem grep no arquivo não passar batido.
/deploy/supabase/.env

# Cópias/experimentos do projeto dentro do próprio projeto.
# (têm .git, .env e data/ próprios — nunca devem entrar neste repositório)
/gamehub-independentlab
/*-independentlab

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Sistema
.DS_Store
Thumbs.db

# Editor
.vscode/*
!.vscode/extensions.json
.idea/
