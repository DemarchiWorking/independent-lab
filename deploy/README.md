# Deploy na VPS Hostinger

> **Movido:** o guia completo (instalar, configurar, executar, Docker,
> Supabase self-hosted, troca file↔supabase, prompt pronto pro Claude Code na
> VPS) agora vive em
> [`../docs/deploy/README.md`](../docs/deploy/README.md) — este arquivo
> ficava desatualizado (chegou a afirmar "sem Docker", que não é mais verdade
> desde o GH-OPS Bloco 2) por estar duplicado em dois lugares.
>
> Para o runbook que o **Claude Code lê automaticamente** ao operar neste
> repositório (local ou na VPS), veja [`../AGENTS.md`](../AGENTS.md).

Os scripts nesta pasta (`vps-setup.sh`, `deploy.sh`, `supabase-up.sh`,
`backup.sh`, `ecosystem.config.js`, `nginx*.conf.template`) são autoexplicativos
via comentário — mas o passo a passo e o "por quê" de cada decisão estão no
guia acima.
